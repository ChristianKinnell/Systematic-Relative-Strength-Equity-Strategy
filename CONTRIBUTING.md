# Contributing

The final strategy definition is frozen. Refactors must preserve outputs and should not silently change investment logic. A change to signals, thresholds, universe rules, sizing, risk controls or validation assumptions requires a new strategy version and changelog entry.
